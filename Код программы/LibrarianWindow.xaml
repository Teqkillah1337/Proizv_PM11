<Window x:Class="LibraryManagementSystem.LibrarianWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library Management System - Librarian" Height="600" Width="800"
        Background="#F0FFF0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Librarian Dashboard" FontSize="24" FontWeight="Bold" 
                  HorizontalAlignment="Center" Margin="20" Foreground="DarkGreen"/>

        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Issue Book">
                <StackPanel Margin="10">
                    <TextBlock Text="Issue Book to Reader" FontSize="16" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Reader ID:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="ReaderIdTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Book ISBN:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="BookIsbnTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

                        <Button Content="Issue Book" Grid.Row="2" Grid.ColumnSpan="2" 
                                Margin="5" Padding="10,5" Click="IssueBook_Click" 
                                Background="ForestGreen" Foreground="White"/>
                    </Grid>
                </StackPanel>
            </TabItem>

            <TabItem Header="Return Book">
                <StackPanel Margin="10">
                    <TextBlock Text="Return Book from Reader" FontSize="16" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Issue ID:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="IssueIdTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                        <Button Content="Return Book" Grid.Row="1" Grid.ColumnSpan="2" 
                                Margin="5" Padding="10,5" Click="ReturnBook_Click" 
                                Background="ForestGreen" Foreground="White"/>
                    </Grid>
                </StackPanel>
            </TabItem>

            <TabItem Header="Register Reader">
                <StackPanel Margin="10">
                    <TextBlock Text="Register New Reader" FontSize="16" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Full Name:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="ReaderNameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Email:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="ReaderEmailTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Phone:" Grid.Row="2" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="ReaderPhoneTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Address:" Grid.Row="3" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="ReaderAddressTextBox" Grid.Row="3" Grid.Column="1" Margin="5"/>

                        <Button Content="Register Reader" Grid.Row="4" Grid.ColumnSpan="2" 
                                Margin="5" Padding="10,5" Click="RegisterReader_Click" 
                                Background="ForestGreen" Foreground="White"/>
                    </Grid>
                </StackPanel>
            </TabItem>

            <TabItem Header="Manage Catalog">
                <StackPanel Margin="10">
                    <TextBlock Text="Book Catalog" FontSize="16" Margin="0,0,0,10"/>
                    <DataGrid x:Name="BooksDataGrid" AutoGenerateColumns="False" Height="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="100"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Year" Binding="{Binding PublishYear}" Width="80"/>
                            <DataGridTextColumn Header="Genre" Binding="{Binding Genre.Name}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Content="Refresh Catalog" Margin="0,10,0,0" Padding="10,5" 
                            Click="RefreshCatalog_Click" Background="ForestGreen" Foreground="White"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Apply Fine">
                <StackPanel Margin="10">
                    <TextBlock Text="Apply Fine to Reader" FontSize="16" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Reader ID:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="FineReaderIdTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Amount:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="FineAmountTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

                        <TextBlock Text="Reason:" Grid.Row="2" Grid.Column="0" Margin="5"/>
                        <TextBox x:Name="FineReasonTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

                        <Button Content="Apply Fine" Grid.Row="3" Grid.ColumnSpan="2" 
                                Margin="5" Padding="10,5" Click="ApplyFine_Click" 
                                Background="ForestGreen" Foreground="White"/>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>

        <Button Grid.Row="2" Content="Logout" HorizontalAlignment="Center" 
                Margin="10" Padding="10,5" Click="Logout_Click" 
                Background="LightGreen" Foreground="DarkGreen"/>
    </Grid>
</Window>