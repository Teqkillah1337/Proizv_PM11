<UserControl x:Class="LibraryManagementSystem.GenerateReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Generate Reports" FontSize="18" FontWeight="Bold" Margin="10"/>

        <ComboBox x:Name="ReportTypeComboBox" Grid.Row="1" Margin="10" Width="200" HorizontalAlignment="Left">
            <ComboBoxItem Content="Books Issued"/>
            <ComboBoxItem Content="Overdue Books"/>
            <ComboBoxItem Content="Popular Books"/>
            <ComboBoxItem Content="Reader Activity"/>
        </ComboBox>

        <DatePicker x:Name="StartDatePicker" Grid.Row="2" Margin="10,0,5,10" Width="150" HorizontalAlignment="Left"/>
        <DatePicker x:Name="EndDatePicker" Grid.Row="2" Margin="170,0,10,10" Width="150" HorizontalAlignment="Left"/>

        <DataGrid x:Name="ReportDataGrid" Grid.Row="3" Margin="10" AutoGenerateColumns="True"/>

        <Button Content="Generate Report" Grid.Row="4" Margin="10" Padding="10,5" 
                HorizontalAlignment="Right" Click="GenerateReport_Click" 
                Background="ForestGreen" Foreground="White"/>
    </Grid>
</UserControl>