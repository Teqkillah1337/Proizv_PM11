<UserControl x:Class="LibraryManagementSystem.SystemSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Максимальное количество книг:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="MaxBooksTextBox" Width="50" HorizontalAlignment="Left"/>
        <!--<Button Grid.Row="1" Grid.Column="1" Content="Save Settings" Click="SaveSettings_Click" HorizontalAlignment="Left" Margin="0,5,0,0"/>-->
    </Grid>
</UserControl>